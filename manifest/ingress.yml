apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deletetodo-ingress

spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: delete-todo-api.dhoomdhoom.shop
    http: 
      paths:  
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: deletetodo
            port:
              number: 80

